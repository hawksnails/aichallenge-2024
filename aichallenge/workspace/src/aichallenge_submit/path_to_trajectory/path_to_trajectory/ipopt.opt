# for more information about options for the solver Ipopt, see 'https://coin-or.github.io/Ipopt/OPTIONS.html'
# and also, this might help: 'https://www.gams.com/latest/docs/S_IPOPT.html'

# the iteration time is limited to the value below (default:3000)
# because iteration time typically becomes large when the solution does not converge
max_iter 50000

# the strategy of updatin mu is set below (default:monotone). 
# As this mu gets smaller, the condition gets more strict, but it is more possible to diverge.
mu_strategy adaptive

# the initial value of mu (default:0.1), only valid when mu_strategy is monotone, critical
mu_init 0.0005

# torelance
tol 1e-4

# 最初muを大きめにとってすぐに減らす戦法
mu_linear_decrease_factor 0.9
# mu_superlinear_decrease_power 1.9

# これが小さいと制約条件について鋭敏に更新しすぎなくなるはず(default:100)
nlp_scaling_constr_target_gradient 0.5

# no print when calclating is needed
# print_level 4